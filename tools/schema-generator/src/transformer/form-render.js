// form-render的schema与fr-generator的schema的相互转换，默认原样转换

// FR Schema => FRG schema
export const fromFormRender = schema => schema;

// FRG schema => FR Schema
export const toFormRender = schema => schema;
